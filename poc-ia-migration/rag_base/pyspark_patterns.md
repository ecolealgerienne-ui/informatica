# PySpark Patterns (Phase 2 — Out of Scope for POC)

This file is reserved for Phase 2 when the target platform is Cloudera or Databricks.

## Scope
- Workflows flagged `routing_decision.target_platform = "pyspark"` or `"databricks"`
- Volume > 5M rows
- Workflows with complex joins, window functions, or partitioned writes

## Status
**NOT USED in this POC.** Python standalone only.
